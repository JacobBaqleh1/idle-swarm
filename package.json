{
  "name": "idle-swarm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint js/",
    "test": "jest",
    "ci": "npm run lint && npm test"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jest": "^29.0.0"
  }
}
